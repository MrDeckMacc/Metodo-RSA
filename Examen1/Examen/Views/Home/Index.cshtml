@{
    ViewData["Title"] = "Registro";
    string mensajeExito = TempData["MensajeExito"] as string;
}

<h1>@ViewData["Title"]</h1>

@if (!string.IsNullOrEmpty(mensajeExito))
{
    <div class="alert alert-success" role="alert">
        @mensajeExito
    </div>
}

<div class="row">
    <div class="col-md-6">
        <form asp-controller="Usuario" asp-action="Registrar" method="post">
            <div class="form-group">
                <label for="nombreUsuario">Nombre de usuario:</label>
                <input type="text" class="form-control" id="nombreUsuario" name="nombreUsuario" required>
            </div>
            <div class="form-group">
                <label for="contraseña">Contraseña:</label>
                <input type="password" class="form-control" id="contraseña" name="contraseña" required>
            </div>
            <button type="submit" class="btn btn-primary">Registar</button>
        </form>
    </div>
</div>

@* @{
    ViewData["Title"] = "Registro";
}
<h1>@ViewData["Title"]</h1>

<form action="/Usuario/Registrar" method="post">
    <div>
        <label for="NombreUsuario">Nombre de usuario:</label>
        <input type="text" id="NombreUsuario" name="NombreUsuario" />
        <!-- Puedes agregar mensajes de validación aquí si lo deseas -->
    </div>
    <div>
        <label for="Contraseña">Contraseña:</label>
        <input type="password" id="Contraseña" name="Contraseña" />
        <!-- Puedes agregar mensajes de validación aquí si lo deseas -->
    </div>
    <button type="submit">Registrar</button>
</form> *@
